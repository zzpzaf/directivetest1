import { MyArrayCheck1Directive } from './my-array-check1.directive';

describe('MyArrayCheck1Directive', () => {
  it('should create an instance', () => {
    const directive = new MyArrayCheck1Directive();
    expect(directive).toBeTruthy();
  });
});
