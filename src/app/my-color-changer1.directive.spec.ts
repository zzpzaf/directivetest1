import { MyColorChanger1Directive } from './my-color-changer1.directive';

describe('MyColorChanger1Directive', () => {
  it('should create an instance', () => {
    const directive = new MyColorChanger1Directive();
    expect(directive).toBeTruthy();
  });
});
